import React from 'react';
import { SpinnerContainer, LoadingSpinner } from './style';

function Spinner() {
  return (
    <SpinnerContainer>
      <LoadingSpinner />
    </SpinnerContainer>
  );
}

export default Spinner;
